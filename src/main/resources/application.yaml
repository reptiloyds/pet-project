jwt:
  issuer: bloghub_app
  secret: se12dd4f1sg321we
  lifetime-in-minutes: 60
server:
  port: 8080
spring:
  application:
    name: bloghub
  datasource:
    url: jdbc:postgresql://localhost:5432/bloghub_dev
    username: postgres
    password: gh5j8DR41
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
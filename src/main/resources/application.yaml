server:
  port: 8080
spring:
  application:
    name: bloghub
  datasource:
    url: jdbc:postgresql://localhost:5432/bloghub_dev
    username: postgres
    password: gh5j8DR41
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true